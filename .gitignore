# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# figma working artifacts (design references)
/figma_data
/figma_refs

# sqlite database (runtime data)
/data

# mail server secrets (Brevo key, mailbox passwords)
/mail_travel/.env

# Claude internal memory (not project code)
/memory

# migration bundle (DKIM key, passwords — never commit)
/mail_travel/migration
/mail_travel/*.tar.gz
